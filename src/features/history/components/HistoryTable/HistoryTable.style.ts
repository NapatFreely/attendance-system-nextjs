import { SxProps } from '@mui/system'

const useStyles = () => {
  const sidebar: SxProps = {
    gridColumn: 'span 10',
    textAlign: 'center',
    padding: '16px',
  }
  return {
    sidebar,
  }
}

export default useStyles
