import { SxProps } from '@mui/system'

const useStyles = () => {
  const form: SxProps = {
    width: '360px',
    textAlign: 'center',
    background: 'white',
    padding: '24px',
    borderRadius: '8px',
  }

  return { form }
}

export default useStyles
